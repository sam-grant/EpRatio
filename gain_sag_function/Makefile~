Target = gain_sag_function.exe
ROOTFLAGS = $(shell root-config --cflags)
ROOTLIBS = $(shell root-config --libs)

all:$(Target)

magic8ball.exe: gain_sag_function.cc
	g++ -o $@ gain_sag_function.exe $(Objects) $(ROOTFLAGS) $(ROOTLIBS)

clear:
	rm *exe
